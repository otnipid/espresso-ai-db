---
postgresql:
  # Minimal base values for chart structure
  # Environment-specific values will override these
  image: postgres:15
  port: 5432
  user: postgres
  password: null
  db: espresso_ml
  storage: 1Gi
