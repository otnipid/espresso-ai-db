---
postgresql:
  # Custom Espresso ML PostgreSQL image with pre-loaded schemas
  image: ghcr.io/espresso-ml/espresso-ml-postgres:latest
  # Fallback to official image if custom image is not available
  # image: postgres:15
  port: 5432
  user: postgres
  password: null
  db: espresso_ml
  storage: 1Gi
