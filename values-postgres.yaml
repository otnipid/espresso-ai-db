auth:
  username: espresso_user
  password: espresso_pass
  database: espresso

primary:
  persistence:
    enabled: true
    size: 10Gi

  resources:
    requests:
      cpu: 250m
      memory: 512Mi

  pgHbaConfiguration: |
    host all all all md5

metrics:
  enabled: false
